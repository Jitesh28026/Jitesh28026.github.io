# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db

# editor folders
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json

# Claude Code per-developer overrides (project-level shared settings stay in .claude/settings.json)
.claude/settings.local.json

# NOTE: public/frames/ (the /flat film's WebP sequences, ~34MB) is intentionally
# COMMITTED. GitHub Pages only serves what is in the repo. Regenerate with
# scripts/extract-frames.sh if the source clips change.

