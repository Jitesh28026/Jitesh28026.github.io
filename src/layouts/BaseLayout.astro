---
import '../styles/tokens.css';
import '../styles/global.css';
import '../styles/legacy.css';
import '../styles/transitions.css';
import Loader from '../components/Loader.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  noindex?: boolean;
}

const {
  title,
  description = 'Jitesh is a product designer based in Bengaluru, crafting thoughtful B2B experiences. Portfolio showcasing UX design, prototyping, and design systems work.',
  ogImage = '/og-image.png',
  noindex = false,
} = Astro.props;

const canonical = new URL(Astro.url.pathname, Astro.site).toString();
const ogImageUrl = new URL(ogImage, Astro.site).toString();

// Person structured data — tells Google that jitesht.com, LinkedIn, Behance,
// Medium and GitHub are all the same person. Feeds Search + the AI Overview.
const PRIMARY_SITE = 'https://jitesht.com';
const personSchema = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Jitesh Thakker',
  alternateName: 'Jitesh',
  url: `${PRIMARY_SITE}/`,
  image: `${PRIMARY_SITE}/og-image.png`,
  jobTitle: 'Product Designer',
  description:
    'Product designer based in Bengaluru, crafting thoughtful B2B experiences with a focus on UX design, prototyping, and design systems.',
  email: 'mailto:thakker.jitesh04@gmail.com',
  address: {
    '@type': 'PostalAddress',
    addressLocality: 'Bengaluru',
    addressRegion: 'Karnataka',
    addressCountry: 'IN',
  },
  sameAs: [
    'https://www.linkedin.com/in/jitesh-thakker/',
    'https://www.behance.net/jiteshthakker',
    'https://medium.com/@thakker.jitesh04',
    'https://github.com/Jitesh28026',
  ],
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex" />}
    <link rel="canonical" href={canonical} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImageUrl} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />

    <script type="application/ld+json" set:html={JSON.stringify(personSchema)} is:inline />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;400;500&family=Space+Grotesk:ital,wght@0,400;0,500;0,700;1,400;1,500&display=swap"
    />

    <script type="text/javascript" is:inline>
      (function(c,l,a,r,i,t,y){
          c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
          t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
          y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", "wrdy72b3sd");
    </script>
  </head>
  <body class="is-loading">
    <Loader />
    <div class="ah-headlights" data-headlights aria-hidden="true"></div>
    <slot />
    <Footer />
    <script>
      import '../scripts/smooth-scroll';
      import '../scripts/reveal';
      import '../scripts/page-transitions';
      import '../scripts/headlights';
    </script>
  </body>
</html>
